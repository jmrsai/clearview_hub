# ClearView Hub — Environment Variables
# Copy this file to .env and fill in your actual keys
# NEVER commit .env to version control!

# Gemini AI API Key — Get free at https://aistudio.google.com
GEMINI_API_KEY=AIzaSyCUyEiLX7u1B2B-b

# Optional: Override Gemini model (default: gemini-1.5-flash)
GEMINI_MODEL=gemini-1.5-flash

# Optional: AI request temperature (0.0 = deterministic, 1.0 = creative)
GEMINI_TEMPERATURE=0.4

# Optional: Max tokens per AI response
GEMINI_MAX_TOKENS=2048
